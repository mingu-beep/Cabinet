<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cabinet.Cabinet.dao.MemberDaoInterface">
	<!-- 회원가입 -->
	<insert id="regMember" parameterType="com.cabinet.Cabinet.dto.MemberDTO">
		insert into TB_MEMBER (memID, memPw, memName, memEmail, memPhone, memAdrs, memBday)
		values (#{memID}, #{memPw}, #{memName}, #{memEmail}, #{memPhone}, #{memAdrs}, #{memBday})
	</insert>

	<select id="existById" parameterType="String" resultType="_boolean">
		select if(count(*) >= 1, 1, 0)
		from TB_MEMBER
		where memID = #{id};
	</select>

	<select id="existByEmail" parameterType="String" resultType="_boolean">
		select if(count(*) >= 1, 1, 0)
		from TB_MEMBER
		where memEmail = #{email};
	</select>

	<!-- 회원 인증키(난수) 생성 -->
	<insert id="getKey" parameterType="Map">
		update memberInfo set member_key = #{param2} where inputId = #{param1}
	</insert>
	<!-- 회원 인증키(난수) Y로 변경 -->
	<update id="alter_memberKey" parameterType="Map">
		update memberInfo set member_key = "Y" where inputId = #{param1} and user_key = #{param2}
	</update>
</mapper>